% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_misfit_scatter.R
\name{plot_misfit_scatter}
\alias{plot_misfit_scatter}
\title{Scatter plot of two person-fit statistics with cut-off lines}
\usage{
plot_misfit_scatter(
  object,
  x_stat = "lpz",
  y_stat = "Gp",
  alpha = 0.05,
  x_cut = NULL,
  y_cut = NULL,
  ...
)
}
\arguments{
\item{object}{An \code{epmfd_misfit} object, or an \code{epmfd_clean} object that
still contains the original misfit results.}

\item{x_stat}{Character, \strong{one of} \code{"lpz"}, \code{"Gp"}, \code{"Gpn"}, \code{"U3p"}.
Statistic to be shown on the \emph{x}-axis.
Default is \code{"lpz"}.}

\item{y_stat}{Character, \strong{one of} \code{"lpz"}, \code{"Gp"}, \code{"Gpn"}, \code{"U3p"},
\strong{different from} \code{x_stat}.  Statistic to be shown on the \emph{y}-axis.
Default is \code{"Gp"}.}

\item{alpha}{Numeric in (0, 1).  If \code{x_cut} or \code{y_cut} is \code{NULL},
cut-off values are set to \code{qnorm(1 - alpha)}.}

\item{x_cut, y_cut}{Numeric cut-off values.  When supplied they override
the \code{alpha}-based defaults.}

\item{...}{Additional aesthetics or layers passed to
\code{\link[ggplot2:geom_point]{ggplot2::geom_point()}}.}
}
\value{
A \code{ggplot} object that can be further customised.
}
\description{
Displays respondents in the plane defined by two misfit statistics.
Points that exceed both cut-off values (upper-right quadrant) are the
most problematic persons.
}
